/**
 * Division catalogue content. Each division is one data object fed to the shared
 * `DivisionPage` template — design lives in `components/karmo/division/`, content
 * lives here. Add a new division by dropping a file next to these and pointing a
 * route at it.
 */

export { default as mattress } from "./mattress";
export { default as foam } from "./foam";
export { default as hometex } from "./hometex";
export { default as chemicals } from "./chemicals";
